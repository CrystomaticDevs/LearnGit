package builder_demo.adapter_demo;

public class placement {
    public void process(candidate candidate){
        // conduct interview
        // if selected in interview
        adapterClass.getEmployee(candidate);
    }
}
